#include <iostream>
#include <cmath>

using namespace std;

int main(){
    int x=89, y=99;
    cout<<"Max: "<<max(x,y) <<"Min: "<<min(x,y);
    cout<<"Sqrt: "<<sqrt(y) <<"LOg: "<<log(x);
}
